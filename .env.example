# Canonical (high-fidelity) auth: an Anthropic API key for the ApiLoopDriver.
ANTHROPIC_API_KEY=sk-ant-...

# Subscription mode (--auth cli) needs no key here — it uses your existing
# `claude` CLI sign-in and is billed to your Claude subscription, not the API.
